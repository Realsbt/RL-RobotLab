# DMBot Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `/home/sbt/Desktop/robot_lab` 里的 DMBot/OpenDog 资产迁移到当前 `/home/sbt/Desktop/go2_rl_robotlab-main`，新增独立的 `RobotLab-DMBot-v0` 任务，并先完成 IsaacLab spawn 验证和平地最小训练验证。

**Architecture:** 不直接修改现有 `tasks/go2/`，而是新增 `tasks/dmbot/`，先复用 Go2 的训练框架、MDP 逻辑和 RSL-RL 配置，再逐步替换机器人资产、关节名、机身/足端 link、默认站姿、奖励参数和训练地形。Go2 预训练权重只作为训练流程参考，不作为 DMBot 的可直接复用模型。

**Tech Stack:** Python 3.11, IsaacLab, RobotLab, RSL-RL, USD assets, PyTorch, pytest, MuJoCo deployment later.

---

## 记录规则

- 每完成一个步骤，就把对应 checkbox 改成 `[x]`，并在“执行记录”里新增一条带日期和结果的记录。
- 每遇到失败，就在“问题记录”里写清楚命令、报错摘要、判断原因和下一步处理方式。
- 迁移期间不直接覆盖 `tasks/go2/`；Go2 当前能跑的基线要保留。
- 每个阶段先做最小验证，再进入下一阶段；资产无法稳定 spawn 前不开始正式训练。

## 当前判断

- 当前项目里已有 Go2 训练任务：`RobotLab-Go2-v0`。
- 当前项目的 Go2 MuJoCo 默认场景是 `resources/go2/stairs_and_slope.xml`，预训练模型在 `deploy/pre_train/go2/`。
- 待迁移资产仓库是 `/home/sbt/Desktop/robot_lab`。
- 已确认待迁移仓库里存在 DMBot/OpenDog 资产和配置：
  - `/home/sbt/Desktop/robot_lab/data/Robots/dm/OpenDog_novlnk/OpenDog_novlnk.usd`
  - `/home/sbt/Desktop/robot_lab/data/Robots/dm/dmgo/dmgo.usd`
  - `/home/sbt/Desktop/robot_lab/source/robot_lab/robot_lab/assets/dmbot.py`
  - `/home/sbt/Desktop/robot_lab/source/robot_lab/robot_lab/assets/custom_actuator.py`
- DMBot 是四足 12 自由度，结构上可以沿用 Go2 的 locomotion 训练框架，但不能直接使用 Go2 的预训练策略。

## 文件结构计划

- Rename: `prompt.md` -> `计划.md`
- Copy: `/home/sbt/Desktop/robot_lab/data/Robots/dm/` -> `resources/Robots/dm/`
- Create: `source/robot_lab/robot_lab/assets/dmbot.py`
- Create: `source/robot_lab/robot_lab/assets/custom_actuator.py`
- Create: `tests/test_dmbot_asset_files.py`
- Create: `scripts/tools/inspect_dmbot_usd.py`
- Create: `source/robot_lab/robot_lab/tasks/dmbot/__init__.py`
- Create: `source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py`
- Create: `source/robot_lab/robot_lab/tasks/dmbot/rsl_rl_cfg.py`
- Copy initially: `source/robot_lab/robot_lab/tasks/go2/mdp/` -> `source/robot_lab/robot_lab/tasks/dmbot/mdp/`
- Copy initially: `source/robot_lab/robot_lab/tasks/go2/env/` -> `source/robot_lab/robot_lab/tasks/dmbot/env/`
- Copy initially: `source/robot_lab/robot_lab/tasks/go2/manager/` -> `source/robot_lab/robot_lab/tasks/dmbot/manager/`

## Task 1: 迁入 DMBot 资产和 actuator 配置

**Files:**
- Create: `resources/Robots/dm/`
- Create: `source/robot_lab/robot_lab/assets/dmbot.py`
- Create: `source/robot_lab/robot_lab/assets/custom_actuator.py`
- Test: `tests/test_dmbot_asset_files.py`

- [x] **Step 1: 写资产存在性测试**

Create `tests/test_dmbot_asset_files.py`:

```python
from pathlib import Path


ROOT = Path(__file__).resolve().parents[1]


def test_dmbot_asset_files_are_present():
    expected = [
        ROOT / "resources/Robots/dm/OpenDog_novlnk/OpenDog_novlnk.usd",
        ROOT / "resources/Robots/dm/OpenDog_novlnk/configuration/OpenDog_novlnk_base.usd",
        ROOT / "resources/Robots/dm/OpenDog_novlnk/configuration/OpenDog_novlnk_physics.usd",
        ROOT / "resources/Robots/dm/OpenDog_novlnk/configuration/OpenDog_novlnk_robot.usd",
        ROOT / "resources/Robots/dm/OpenDog_novlnk/configuration/OpenDog_novlnk_sensor.usd",
        ROOT / "resources/Robots/dm/dmgo/dmgo.usd",
        ROOT / "source/robot_lab/robot_lab/assets/dmbot.py",
        ROOT / "source/robot_lab/robot_lab/assets/custom_actuator.py",
    ]
    missing = [str(path.relative_to(ROOT)) for path in expected if not path.exists()]
    assert missing == []


def test_dmbot_asset_config_references_project_asset_dir():
    text = (ROOT / "source/robot_lab/robot_lab/assets/dmbot.py").read_text()
    assert "DM_CFG" in text
    assert "ISAACLAB_ASSETS_DATA_DIR" in text
    assert "PhysicalMotorCfg" in text
    assert "OpenDog_novlnk/OpenDog_novlnk.usd" in text
```

- [x] **Step 2: 运行测试确认迁入前失败**

Run:

```bash
conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py -q
```

Expected before copy: FAIL, missing DMBot files.

- [x] **Step 3: 复制资产和 actuator 文件**

Run:

```bash
mkdir -p resources/Robots
cp -a /home/sbt/Desktop/robot_lab/data/Robots/dm resources/Robots/
cp /home/sbt/Desktop/robot_lab/source/robot_lab/robot_lab/assets/dmbot.py source/robot_lab/robot_lab/assets/dmbot.py
cp /home/sbt/Desktop/robot_lab/source/robot_lab/robot_lab/assets/custom_actuator.py source/robot_lab/robot_lab/assets/custom_actuator.py
```

- [x] **Step 4: 运行测试确认资产迁入成功**

Run:

```bash
conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py -q
```

Expected after copy: PASS.

## Task 2: 读取 USD 中的真实 body/joint 名称

**Files:**
- Create: `scripts/tools/inspect_dmbot_usd.py`
- Update: `计划.md`

- [x] **Step 1: 添加 USD 检查脚本**

Create `scripts/tools/inspect_dmbot_usd.py`:

```python
from pathlib import Path
import sys

from isaaclab.app import AppLauncher

app = AppLauncher(headless=True).app

from pxr import Usd, UsdPhysics


ROOT = Path(__file__).resolve().parents[2]
USD_PATH = ROOT / "resources/Robots/dm/OpenDog_novlnk/OpenDog_novlnk.usd"


def main():
    stage = Usd.Stage.Open(str(USD_PATH))
    if stage is None:
        raise SystemExit(f"Could not open USD: {USD_PATH}")

    rigid_bodies = []
    joints = []
    articulation_roots = []

    for prim in stage.Traverse():
        path = prim.GetPath().pathString
        if prim.HasAPI(UsdPhysics.RigidBodyAPI):
            rigid_bodies.append(path)
        if prim.HasAPI(UsdPhysics.ArticulationRootAPI):
            articulation_roots.append(path)
        if prim.IsA(UsdPhysics.Joint):
            joints.append(path)

    print("USD:", USD_PATH)
    print("\nArticulation roots:")
    for item in articulation_roots:
        print(" ", item)
    print("\nRigid bodies:")
    for item in rigid_bodies:
        print(" ", item)
    print("\nJoints:")
    for item in joints:
        print(" ", item)


if __name__ == "__main__":
    try:
        main()
    finally:
        sys.stdout.flush()
        app.close()
```

- [x] **Step 2: 运行 USD 检查脚本**

Run:

```bash
conda run -n env_isaaclab python scripts/tools/inspect_dmbot_usd.py
```

Expected: 输出 articulation root、rigid bodies、joints。把真实 base link、foot link 和 12 个驱动关节名称记录到本文件“执行记录”。

- [x] **Step 3: 根据输出锁定 DMBot 环境常量**

Expected constants unless USD 输出证明需要调整:

```python
JOINT_NAMES = [
    "Jfl1_hipr", "Jfl2_hipp", "Jfl3_knee",
    "Jfr1_hipr", "Jfr2_hipp", "Jfr3_knee",
    "Jrl1_hipr", "Jrl2_hipp", "Jrl3_knee",
    "Jrr1_hipr", "Jrr2_hipp", "Jrr3_knee",
]

BASE_HEIGHT_TARGET = 0.35
```

The `BASE_LINK_NAME` and `FOOT_LINK_NAME` must be taken from the USD/script output before writing `env_cfg.py`.

## Task 3: 新增独立 DMBot IsaacLab 任务

**Files:**
- Create: `source/robot_lab/robot_lab/tasks/dmbot/__init__.py`
- Create: `source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py`
- Create: `source/robot_lab/robot_lab/tasks/dmbot/rsl_rl_cfg.py`
- Create/copy: `source/robot_lab/robot_lab/tasks/dmbot/mdp/`
- Create/copy: `source/robot_lab/robot_lab/tasks/dmbot/env/`
- Create/copy: `source/robot_lab/robot_lab/tasks/dmbot/manager/`

- [x] **Step 1: 复制 Go2 任务作为 DMBot 初始骨架**

Run:

```bash
cp -a source/robot_lab/robot_lab/tasks/go2 source/robot_lab/robot_lab/tasks/dmbot
```

- [x] **Step 2: 修改 DMBot 任务注册**

In `source/robot_lab/robot_lab/tasks/dmbot/__init__.py`, register:

```python
gym.register(
    id="RobotLab-DMBot-v0",
    entry_point="robot_lab.tasks.dmbot.env.go2_env:Go2Env",
    disable_env_checker=True,
    kwargs={
        "env_cfg_entry_point": f"{__name__}.env_cfg:DMBotEnvCfg",
        "rsl_rl_cfg_entry_point": f"{__name__}.rsl_rl_cfg:MoECTSRunnerCfg",
    },
)
```

- [x] **Step 3: 修改 `env_cfg.py` 的机器人配置**

Required changes:

```python
import robot_lab.tasks.dmbot.mdp as mdp
from robot_lab.assets.dmbot import DM_CFG
from robot_lab.tasks.dmbot.mdp.terrains import TERRAIN_CFG

JOINT_NAMES = [
    "Jfl1_hipr", "Jfl2_hipp", "Jfl3_knee",
    "Jfr1_hipr", "Jfr2_hipp", "Jfr3_knee",
    "Jrl1_hipr", "Jrl2_hipp", "Jrl3_knee",
    "Jrr1_hipr", "Jrr2_hipp", "Jrr3_knee",
]

BASE_HEIGHT_TARGET = 0.35
```

Also change:

```python
robot: ArticulationCfg = DM_CFG.replace(prim_path="{ENV_REGEX_NS}/Robot")
```

Use the real `BASE_LINK_NAME` and `FOOT_LINK_NAME` discovered in Task 2.

- [x] **Step 4: 修改 action scale 的关节匹配规则**

Use DMBot joint suffixes:

```python
scale={".*_hipr": 0.25, ".*_hipp": 0.25, ".*_knee": 0.25}
```

- [x] **Step 5: 修改 RSL-RL 实验名**

In `source/robot_lab/robot_lab/tasks/dmbot/rsl_rl_cfg.py`, set:

```python
experiment_name = "dmbot_moe_cts"
```

## Task 4: 注册和 spawn smoke test

**Files:**
- Update: `计划.md`

- [x] **Step 1: 验证 Gym task 可注册**

Run:

```bash
conda run -n env_isaaclab python -c "from isaaclab.app import AppLauncher; app=AppLauncher(headless=True).app; import gymnasium as gym; import robot_lab.tasks; spec=gym.spec('RobotLab-DMBot-v0'); print(spec.id, spec.entry_point); app.close()"
```

Expected: 输出 `RobotLab-DMBot-v0` 和 DMBot entry point.

- [x] **Step 2: 做最小 headless 训练 smoke test**

Run:

```bash
conda run -n env_isaaclab python scripts/rsl_rl/train.py --task=RobotLab-DMBot-v0 --headless --num_envs=4 --max_iterations=1
```

Expected: 环境能够创建、reset、跑完 1 iteration，并写出 `logs/rsl_rl/dmbot_moe_cts/.../model_0.pt`。

- [x] **Step 3: 如果 spawn 失败，记录并修正**

Common fixes:

```text
SceneEntityCfg 找不到 body: 修正 BASE_LINK_NAME 或 FOOT_LINK_NAME。
Action/observation joint name mismatch: 修正 JOINT_NAMES 顺序或正则。
USD cannot open: 修正 resources/Robots/dm 路径或 dmbot.py 的 usd_path。
Actuator import error: 确认 custom_actuator.py 已迁入并能被 robot_lab.assets.custom_actuator 导入。
```

## Task 5: 平地最小训练

**Files:**
- Update: `source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py`
- Update: `计划.md`

- [x] **Step 1: 将 DMBot 训练地形先限制为 flat/simple terrain**

Use a minimal terrain configuration for first training. The first target is stability and command response, not stairs/slope performance.

- [x] **Step 2: 运行短训练**

Run:

```bash
conda run -n env_isaaclab python scripts/rsl_rl/train.py --task=RobotLab-DMBot-v0 --headless --num_envs=256 --max_iterations=1000
```

Expected: 不持续启动即摔倒，episode reward 不出现明显异常爆炸，能生成 checkpoints。

- [x] **Step 3: 记录训练结果**

Record:

```text
log directory:
last iteration:
mean reward:
mean episode length:
fall/reset pattern:
checkpoint path:
```

## Task 6: 逐步打开复杂地形和域随机化

**Files:**
- Update: `source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py`
- Update: `计划.md`

- [ ] **Step 1: 开启 rough terrain**
- [ ] **Step 2: 开启 slope**
- [ ] **Step 3: 开启 stairs**
- [ ] **Step 4: 开启 push randomization**
- [ ] **Step 5: 开启 mass/friction randomization**
- [ ] **Step 6: 每打开一项都跑一次短训练并记录结果**

Run after each change:

```bash
conda run -n env_isaaclab python scripts/rsl_rl/train.py --task=RobotLab-DMBot-v0 --headless --num_envs=256 --max_iterations=1000
```

Expected: 每次只引入一个新增难度，方便判断是哪一项导致训练不稳定。

## Task 7: MuJoCo Sim2Sim 部署

**Files:**
- Create: `resources/dmbot/`
- Create: `deploy/deploy_mujoco/configs/dmbot.yaml`
- Update: `deploy/deploy_mujoco/` only if DMBot joint order or command mapping needs extra support.
- Update: `计划.md`

- [ ] **Step 1: 准备 DMBot MuJoCo XML**
- [ ] **Step 2: 写 DMBot deploy config**
- [ ] **Step 3: 对齐 IsaacLab 和 MuJoCo 的关节顺序、默认角度、PD 参数**
- [ ] **Step 4: 加载 DMBot checkpoint 做 MuJoCo 运行测试**

Expected: MuJoCo 里能加载 DMBot 模型，策略输入输出维度一致，机器人能响应速度命令。

## 执行记录

- [x] 2026-06-12: 将 `prompt.md` 改名为 `计划.md`，并把迁移路线整理成可追踪计划。当前只形成计划，尚未开始复制 DMBot 资产或改任务代码。

- [x] 2026-06-12: Task 1 Step 1-2 完成。新增 `tests/test_dmbot_asset_files.py`，运行 `conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py -q`，测试按预期失败；失败原因是当前仓库缺少 DMBot USD 资产、`dmbot.py` 和 `custom_actuator.py`。

- [x] 2026-06-12: Task 1 Step 3-4 完成。已复制 `/home/sbt/Desktop/robot_lab/data/Robots/dm` 到 `resources/Robots/dm`，并复制 `dmbot.py`、`custom_actuator.py` 到当前仓库；重新运行 `conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py -q`，结果 `2 passed`。

- [x] 2026-06-12: Task 2 完成。已新增并运行 `scripts/tools/inspect_dmbot_usd.py`。USD 输出显示 articulation root/base rigid body 为 `/DaMiao_OpenDog_novlnk/L0_torso`；足端 rigid bodies 为 `Lfl4_foot`、`Lfr4_foot`、`Lrl4_foot`、`Lrr4_foot`；动作关节使用 12 个 `Jfl/Jfr/Jrl/Jrr` 的 `hipr/hipp/knee`，USD 中额外的 `J*4_foot` 不进入 action joint list。后续常量锁定为 `BASE_LINK_NAME = "L0_torso"`、`FOOT_LINK_NAME = ".*_foot"`、`BASE_HEIGHT_TARGET = 0.35`。

- [x] 2026-06-12: Task 3 完成。已复制 `tasks/go2` 为 `tasks/dmbot`，注册 `RobotLab-DMBot-v0`，替换为 `DM_CFG`、`L0_torso`、`.*_foot`、12 个 DMBot action joints、DMBot action scale 和 `dmbot_moe_cts` 实验名；新增 `tests/test_dmbot_task_config.py`，运行结果 `3 passed`。

- [x] 2026-06-12: Task 4 Step 1 完成。运行 IsaacLab headless 注册检查，`gym.spec('RobotLab-DMBot-v0')` 输出 `RobotLab-DMBot-v0 robot_lab.tasks.dmbot.env.go2_env:Go2Env`。第一次命令未 flush 导致业务输出不可见，补充 `sys.stdout.flush()` 后确认注册成功。

- [x] 2026-06-12: 修正 DMBot 资产加载路径。当前项目 `robot_lab.assets.ISAACLAB_ASSETS_DATA_DIR` 指向仓库根目录 `resources`，因此已补复制资产到 `resources/Robots/dm`，并更新 `tests/test_dmbot_asset_files.py` 与 `scripts/tools/inspect_dmbot_usd.py`。重新运行 DMBot 快速测试结果 `5 passed`，USD 检查脚本确认读取 `/home/sbt/Desktop/go2_rl_robotlab-main/resources/Robots/dm/OpenDog_novlnk/OpenDog_novlnk.usd`。

- [x] 2026-06-12: Task 4 Step 2-3 完成。运行 `conda run -n env_isaaclab python scripts/rsl_rl/train.py --task=RobotLab-DMBot-v0 --headless --num_envs=4 --max_iterations=1` 成功。环境创建成功，action shape 为 12，policy obs shape 为 450，critic obs shape 为 275，跑完 `Learning iteration 0/1`，生成 checkpoint `logs/rsl_rl/dmbot_moe_cts/2026-06-12_15-01-04/model_0.pt`。

- [x] 2026-06-12: 清理冗余资产副本。删除了早先错误复制的 `source/robot_lab/data`，当前保留并使用 `resources/Robots/dm`。

- [x] 2026-06-12: 最终验证完成。重新运行 `conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py tests/test_dmbot_task_config.py -q`，结果 `5 passed`；重新运行 DMBot smoke test `--num_envs=4 --max_iterations=1`，成功生成 `logs/rsl_rl/dmbot_moe_cts/2026-06-12_15-03-13/model_0.pt`。

- [x] 2026-06-12: wandb 连接测试完成。用户已登录 wandb 后，运行 `conda run -n env_isaaclab python scripts/rsl_rl/train.py --task=RobotLab-DMBot-v0 --headless --num_envs=4 --max_iterations=1 --logger wandb --log_project_name Robot_house --run_name wandb_smoke` 成功；项目地址为 `https://wandb.ai/0592bt-carnegie-mellon-university/Robot_house`，run 地址为 `https://wandb.ai/0592bt-carnegie-mellon-university/Robot_house/runs/07oxznej`。

- [x] 2026-06-12: 资产可视化站姿检查完成。新增 `scripts/tools/visual_check_dmbot.py` 和 `tests/test_visual_check_script.py`。运行 `conda run -n env_isaaclab python scripts/tools/visual_check_dmbot.py --duration 60`，GUI 启动成功并保持 60 秒；零动作下 `t=0s` base 高度约 `0.345m`，到 `t=5s` 后 base 高度接近 `0` 且 root quaternion 接近翻倒姿态，说明当前 DMBot 默认站姿/关节方向/PD 保持参数在物理中不能稳定站住。


- [x] 2026-06-12: 按用户要求再次启动 GUI 复查。运行 `conda run -n env_isaaclab python scripts/tools/visual_check_dmbot.py --duration 0`，窗口启动后用户可见；日志显示 `t=0.0s` base 高度约 `0.346m`、root quaternion 接近单位姿态，`t=5.0s` base 高度接近 `0`、root quaternion 接近翻倒姿态。结论保持不变：资产不是出生时整体倒置，而是默认零动作站姿进入仿真后很快翻倒。


- [x] 2026-06-12: 启动 Go2 初始姿态 GUI 对比。运行 `conda run -n env_isaaclab python scripts/tools/visual_check_dmbot.py --task RobotLab-Go2-v0 --duration 60`。Go2 在 `t=0.0s` base 高度约 `0.412m`，姿态接近单位四元数；`t=5s` 后 base 高度约 `0.270m`，仍保持正向直立，没有出现 DMBot 那种翻到背面的情况。


- [x] 2026-06-12: 根据用户确认的根因，已将 DMBot 默认关节站姿改为 Go2 站姿语义映射：hipr 使用 Go2 左右外摆符号，hipp 使用前腿 `0.8`、后腿 `1.0` 且右侧取反，knee 使用左侧 `-1.5`、右侧 `1.5`。新增 `tests/test_dmbot_asset_files.py` 默认站姿断言。验证结果：新增测试先在旧姿态上失败；修改后 `conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py tests/test_dmbot_task_config.py tests/test_visual_check_script.py -q` 结果 `7 passed`；`visual_check_dmbot.py --headless --duration 16` 显示 `t=15s` root quaternion 仍接近正向、未再翻到背面，但 base 高度稳定在约 `0.152m`，说明当前是较低蹲姿；DMBot 1 iteration 训练烟测也成功完成，生成 `logs/rsl_rl/dmbot_moe_cts/2026-06-12_16-36-13/model_0.pt`。


- [x] 2026-06-12: Task 5 平地最小训练完成。已将 DMBot active `TERRAIN_CFG` 改为 generator 路径下 100% `flat`，并将 `DMBotSceneCfg.terrain.max_init_terrain_level` 设为 `0`；新增测试覆盖 flat terrain 配置。验证：`conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py tests/test_dmbot_task_config.py tests/test_visual_check_script.py -q` 结果 `8 passed`；`visual_check_dmbot.py --headless --duration 16` 成功，`t=15s` root quaternion 接近正向、base 高度约 `0.152m`。短训命令 `python scripts/rsl_rl/train.py --task=RobotLab-DMBot-v0 --headless --num_envs=64 --max_iterations=100 --logger wandb --log_project_name Robot_house --run_name dmbot_pose_test_100` 成功跑到 iteration 99/100；日志目录 `logs/rsl_rl/dmbot_moe_cts/2026-06-12_16-51-45_dmbot_pose_test_100`，checkpoint `model_99.pt`，wandb run `https://wandb.ai/0592bt-carnegie-mellon-university/Robot_house/runs/jg0idcxn`。最终指标：Mean teacher reward `-0.77`，Mean teacher episode length `16.31`，Mean student reward `-0.91`，Mean student episode length `19.47`，Curriculum terrain level `0.0`。


- [x] 2026-06-12: 按用户要求撤回 Go2-like 默认站姿尝试，DMBot `DM_CFG.init_state.joint_pos` 已退回 `/home/sbt/Desktop/robot_lab/source/robot_lab/robot_lab/assets/dmbot.py` 的原始参数：`hipr=0.0`，左侧 `hipp=-0.6/knee=-1.0`，右侧 `hipp=0.6/knee=1.0`。同步更新 `tests/test_dmbot_asset_files.py` 断言为源仓库姿态；新增测试先在 Go2-like 姿态上失败，确认断言有效。


- [x] 2026-06-12: 按用户确认的安全方案创建 DMBot USD 姿态副本。已复制 `resources/Robots/dm/OpenDog_novlnk` 为 `resources/Robots/dm/OpenDog_novlnk_stance`，保留原始 USD 不动；新增 `scripts/tools/write_dmbot_usd_stance.py`，向副本 USD 的 12 个可动关节写入 `UsdPhysics.JointStateAPI` 的 `state:angular:physics:position`，数值由当前 `DM_CFG.init_state.joint_pos` 从弧度转换为角度；`source/robot_lab/robot_lab/assets/dmbot.py` 和 `scripts/tools/inspect_dmbot_usd.py` 已改为默认使用 stance 副本。验证：新增/更新测试先在副本不存在、路径未切换时失败；实现后运行 `conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py tests/test_dmbot_task_config.py tests/test_visual_check_script.py tests/test_dmbot_usd_stance_script.py -q`，结果 `11 passed`；运行 `conda run -n env_isaaclab python scripts/tools/write_dmbot_usd_stance.py --verify-only` 退出码为 0；运行 `conda run -n env_isaaclab python scripts/tools/inspect_dmbot_usd.py` 确认打开 `resources/Robots/dm/OpenDog_novlnk_stance/OpenDog_novlnk.usd`；运行 `visual_check_dmbot.py --headless --duration 1 --no-real-time` 成功创建 `RobotLab-DMBot-v0`，`t=0.0s` base 高度约 `0.345m`。


- [x] 2026-06-12: 按用户要求将 DMBot 的 `dmbot.py joint_pos` 初始形态烘焙为 stance USD 的静态 body transform。新增 `scripts/tools/bake_dmbot_usd_stance.py`：默认用原始未烘焙 USD `resources/Robots/dm/OpenDog_novlnk/OpenDog_novlnk.usd` 创建 IsaacLab 环境，reset 到当前 `DM_CFG.init_state.joint_pos`，读取 `robot.data.body_link_state_w`，通过 `subtract_frame_transforms` 转成相对 `L0_torso` 的 body transforms，再写入 `resources/Robots/dm/OpenDog_novlnk_stance/OpenDog_novlnk.usd` 的 `UsdGeom.Xformable` transform op。这样直接打开 stance USD 时，应能看到与 `dmbot.py joint_pos` 对应的静态姿态；原始 USD 仍未修改。验证：新增测试先因 `bake_dmbot_usd_stance.py` 不存在失败；实现后 `conda run -n env_isaaclab pytest tests/test_dmbot_usd_stance_script.py -q` 结果 `3 passed`；执行 `conda run -n env_isaaclab python scripts/tools/bake_dmbot_usd_stance.py` 成功写入并验证 `17` 个 body transform；执行 `conda run -n env_isaaclab python scripts/tools/bake_dmbot_usd_stance.py --verify-only` 退出码为 0；执行 `conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py tests/test_dmbot_task_config.py tests/test_visual_check_script.py tests/test_dmbot_usd_stance_script.py -q` 结果 `12 passed`；执行 `visual_check_dmbot.py --headless --duration 1 --no-real-time` 成功加载，`t=0.0s` base 高度约 `0.346m`。


- [x] 2026-06-12: 针对用户反馈“直接打开 stance USD 仍看不到变化”，补充导出可视化用 flattened USD。判断：`OpenDog_novlnk.usd` 是 8KB 左右的组合层，几何主要来自引用文件，Isaac Sim 直接打开时可能仍显示组合/缓存后的视觉结果，不便确认 overlay 姿态。新增 `scripts/tools/flatten_dmbot_usd_stance.py`，将 `resources/Robots/dm/OpenDog_novlnk_stance/OpenDog_novlnk.usd` flatten 导出为 `resources/Robots/dm/OpenDog_novlnk_stance/OpenDog_novlnk_stance_flattened.usd`。验证：新增测试先因脚本不存在失败；实现后 `conda run -n env_isaaclab pytest tests/test_dmbot_usd_stance_script.py -q` 结果 `4 passed`；执行 `conda run -n env_isaaclab python scripts/tools/flatten_dmbot_usd_stance.py` 成功；文件大小对比：主 stance USD `8.0K`，flattened USD `234M`，说明引用内容已合入单文件。下一次人工直接查看姿态，优先打开 `OpenDog_novlnk_stance_flattened.usd`。

- [x] 2026-06-12: 按用户要求新增 Go2 原始站姿角度的 DMBot USD 预览，不覆盖当前训练用 stance USD。新增 `scripts/tools/bake_dmbot_usd_go2_pose.py`，从原始 `resources/Robots/dm/OpenDog_novlnk/OpenDog_novlnk.usd` 创建 IsaacLab reset，临时使用 Go2 原始站姿语义映射：左侧 `hipr=0.1`、右侧 `hipr=-0.1`，前腿 `hipp=0.8`，后腿 `hipp=1.0`，DMBot 右侧 `hipp/knee` 按镜像轴取反，`knee` 为左侧 `-1.5`、右侧 `1.5`；再将相对 `L0_torso` 的 body transforms 和 `UsdPhysics.JointStateAPI` 写入独立目录 `resources/Robots/dm/OpenDog_novlnk_go2_pose/OpenDog_novlnk.usd`。另新增 `scripts/tools/flatten_dmbot_usd_go2_pose.py`，导出可直接检查的 `resources/Robots/dm/OpenDog_novlnk_go2_pose/OpenDog_novlnk_go2_pose_flattened.usd`。验证：新增测试先因脚本不存在失败；实现后 `conda run -n env_isaaclab pytest tests/test_dmbot_usd_stance_script.py::test_dmbot_usd_go2_pose_script_exports_separate_preview_usd -q` 和 `::test_dmbot_usd_go2_pose_flatten_script_exports_viewable_preview_usd -q` 均通过；执行 `conda run -n env_isaaclab python scripts/tools/bake_dmbot_usd_go2_pose.py` 生成 Go2 pose USD；执行 `conda run -n env_isaaclab python scripts/tools/bake_dmbot_usd_go2_pose.py --verify-only --no-flatten` 退出码为 0；执行 `conda run -n env_isaaclab python scripts/tools/flatten_dmbot_usd_go2_pose.py` 成功，flattened 文件大小 `234M`；最终运行 `conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py tests/test_dmbot_task_config.py tests/test_visual_check_script.py tests/test_dmbot_usd_stance_script.py -q` 结果 `15 passed`。当前 `source/robot_lab/robot_lab/assets/dmbot.py` 未切到 Go2 pose，训练仍默认使用 `OpenDog_novlnk_stance/OpenDog_novlnk.usd`。

- [x] 2026-06-15: 修复 flattened USD 导出格式。用户打开 `OpenDog_novlnk_go2_pose_flattened.usd` 时 Isaac Sim 报 `syntax error at '�'`，确认旧文件是 ASCII USDA 但包含二进制 mesh 数据，不能作为可打开预览文件使用。已将 `scripts/tools/flatten_dmbot_usd_go2_pose.py` 和 `scripts/tools/flatten_dmbot_usd_stance.py` 的输出改为 `.usdc`，并删除 `flattened_layer.identifier = str(TARGET_USD)`，避免 `UsdUsdcFileFormat::SaveToFile with non-Crate-backed layer`；同时两个纯 flatten 脚本不再主动调用 `app.close()`，避免导出异常被 Isaac close 流程掩盖。验证：相关测试先按 `.usdc` 和 no-identifier/no-close 约束失败；修复后 `conda run -n env_isaaclab pytest tests/test_dmbot_usd_stance_script.py -q` 通过；重新执行 `conda run -n env_isaaclab python -u scripts/tools/flatten_dmbot_usd_go2_pose.py` 和 `conda run -n env_isaaclab python -u scripts/tools/flatten_dmbot_usd_stance.py` 成功生成 `OpenDog_novlnk_go2_pose_flattened.usdc` 与 `OpenDog_novlnk_stance_flattened.usdc`；`file` 确认二者均为 `USD crate, version 0.9.0`；最终 `conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py tests/test_dmbot_task_config.py tests/test_visual_check_script.py tests/test_dmbot_usd_stance_script.py -q` 结果 `15 passed`。后续人工查看请打开 `.usdc`，不要再打开旧的 `.usd` flattened 文件。

- [x] 2026-06-15: 按用户要求统一 flattened 预览文件后缀为 `.usd`，但保持实际内容为二进制 USD crate。已将 `scripts/tools/flatten_dmbot_usd_go2_pose.py`、`scripts/tools/flatten_dmbot_usd_stance.py`、`scripts/tools/bake_dmbot_usd_go2_pose.py` 的 flattened 目标从 `.usdc` 改回 `.usd`，同时保留 `Export(..., args={"format": "usdc"})`，确保同名 `.usd` 文件实际是 crate 格式而不是 ASCII USDA。测试新增约束：不再出现 `*_flattened.usdc`，必须存在 `args={"format": "usdc"}`，且不设置 `.identifier`。验证：测试先在 `.usdc` 路径上失败；修复后 `conda run -n env_isaaclab pytest tests/test_dmbot_usd_stance_script.py -q` 结果 `6 passed`；重新执行 `conda run -n env_isaaclab python -u scripts/tools/flatten_dmbot_usd_go2_pose.py` 与 `conda run -n env_isaaclab python -u scripts/tools/flatten_dmbot_usd_stance.py` 成功覆盖生成同名 `.usd`；`file resources/Robots/dm/OpenDog_novlnk_go2_pose/OpenDog_novlnk_go2_pose_flattened.usd resources/Robots/dm/OpenDog_novlnk_stance/OpenDog_novlnk_stance_flattened.usd` 输出二者均为 `USD crate, version 0.9.0`；最终 `conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py tests/test_dmbot_task_config.py tests/test_visual_check_script.py tests/test_dmbot_usd_stance_script.py -q` 结果 `15 passed`。后续人工查看统一打开 `.usd` flattened 文件。

- [x] 2026-06-15: 新增 DMBot 关节标定工具。新增 `scripts/tools/calibrate_dmbot_joints.py`，用于 GUI 中固定 DMBot base、关闭 gravity/随机化/终止项，用键盘逐个调节 12 个动作关节的 position target，并在终端打印可复制到 `dmbot.py` 的 `joint_pos={...}`。键位：`[`/`]` 切换上一个/下一个关节，`a`/`d` 小步减/加，`A`/`D` 5 倍步长减/加，`0` 重置当前关节，`r` 重置全部，`p` 打印当前字典，`q` 退出。新增 `tests/test_dmbot_joint_calibration_script.py`；测试先因脚本不存在失败，脚本实现后 `conda run -n env_isaaclab pytest tests/test_dmbot_joint_calibration_script.py -q` 结果 `3 passed`；`python -m py_compile scripts/tools/calibrate_dmbot_joints.py` 通过；`conda run -n env_isaaclab pytest tests/test_dmbot_joint_calibration_script.py tests/test_visual_check_script.py -q` 结果 `4 passed`。该脚本不修改训练配置、不写 USD，只用于人工找 DMBot 自己的稳定初始站姿。

- [x] 2026-06-15: 修复 DMBot 关节标定工具启动后卡住的问题。原因是脚本曾把任务地形强制改成直接 `plane` 并清空 terrain generator，但 DMBot 的 command manager、height scanner 和 terrain-aware 配置仍依赖 generator 元数据；表现为 Isaac Sim 打印 `Time taken for simulation start` 后不继续进入环境 setup。已改为保留任务自己的 flat terrain generator，只关闭 terrain curriculum。新增回归测试约束校准脚本不能清空 terrain generator；验证：测试先在旧脚本上失败，修复后 `conda run -n env_isaaclab pytest tests/test_dmbot_joint_calibration_script.py -q` 结果 `4 passed`；`python -m py_compile scripts/tools/calibrate_dmbot_joints.py` 通过；`timeout 90s conda run -n env_isaaclab python scripts/tools/calibrate_dmbot_joints.py --headless --duration 0.2 --no-real-time` 退出码 0，并成功打印 `Completed setting up the environment` 与默认 `joint_pos={...}`。

- [x] 2026-06-15: 完成 DMBot 第一轮 1000 iteration 训练。命令：`conda run -n env_isaaclab python scripts/rsl_rl/train.py --task=RobotLab-DMBot-v0 --headless --num_envs=256 --max_iterations=1000 --logger wandb --log_project_name Robot_house --run_name dmbot_train_1000_256envs`。训练正常退出，日志目录 `logs/rsl_rl/dmbot_moe_cts/2026-06-15_11-38-21_dmbot_train_1000_256envs`，checkpoint 已生成 `model_0.pt`、`model_500.pt`、`model_999.pt`；wandb run 地址 `https://wandb.ai/0592bt-carnegie-mellon-university/Robot_house/runs/7ws0ylm0`。最终 iteration 999 指标：Mean teacher reward `5.19`，Mean teacher episode length `906.16`，Mean student reward `5.32`，Mean student episode length `894.76`，`Episode_Termination/time_out=0.5488`，`Episode_Termination/illegal_contact=0.4512`，terrain level 仍为 `0.0`。结论：当前 DMBot 任务已能正常训练并获得正收益，但非法接触仍偏高，下一步应加载 `model_999.pt` 做 GUI 回放判断步态质量，再决定是否调整 illegal contact/base height/姿态或奖励。

- [x] 2026-06-15: 定位 DMBot policy 回放窗口自动关闭的问题。使用 `--num_envs=1` 回放 MoE-CTS checkpoint 会在 `MoECTS.__init__` 触发断言：`len(self.teacher_env_idxs)=0 != self.teacher_num_envs=1`；根因是当前 CTS 算法需要 teacher/student 两类环境，单环境无法分组。验证：`timeout 60s ... play.py --num_envs=1 ... --headless` 复现退出并打印上述 assertion；改为 `--num_envs=2` 后 headless 回放没有快速报错，持续运行到 35 秒 timeout。后续 GUI 回放至少使用 `--num_envs=2`。

- [x] 2026-06-15: 修复 DMBot policy 回放相机距离过远的问题。根因是 `scripts/rsl_rl/play.py` 只依赖 Isaac Sim 默认视角，且 `--fix_commands` 会让机器人持续向前走，启动时的固定相机也会很快被机器人甩开。已在 `play.py` 中新增默认相机跟随：每个仿真 step 读取 `env_0` 的 `robot.data.root_pos_w[0]`，将 viewport camera 设置到机器人后上方并看向 base 附近；新增 `--no-camera-follow` 可关闭。新增 `tests/test_play_camera_follow.py`；测试先因功能不存在失败，修复后 `conda run -n env_isaaclab pytest tests/test_play_camera_follow.py -q` 结果 `1 passed`；`python -m py_compile scripts/rsl_rl/play.py` 通过；`timeout 35s conda run -n env_isaaclab python scripts/rsl_rl/play.py --task=RobotLab-DMBot-v0 --num_envs=2 --checkpoint logs/rsl_rl/dmbot_moe_cts/2026-06-15_11-38-21_dmbot_train_1000_256envs/model_999.pt --headless --real-time --fix_commands` 持续运行到 timeout，没有提前报错或残留进程。

- [x] 2026-06-15: 完成 DMBot 从 `model_999.pt` 继续训练到约 3000 iteration。命令：`conda run -n env_isaaclab python scripts/rsl_rl/train.py --task=RobotLab-DMBot-v0 --headless --num_envs=256 --max_iterations=2000 --resume --load_run 2026-06-15_11-38-21_dmbot_train_1000_256envs --checkpoint model_999.pt --logger wandb --log_project_name Robot_house --run_name dmbot_resume_to_3000_256envs`。训练正常退出，续训日志目录为 `logs/rsl_rl/dmbot_moe_cts/2026-06-15_13-07-09_dmbot_resume_to_3000_256envs`，checkpoint 已生成 `model_1000.pt`、`model_1500.pt`、`model_2000.pt`、`model_2500.pt`、`model_2998.pt`；wandb run 地址 `https://wandb.ai/0592bt-carnegie-mellon-university/Robot_house/runs/l32oyyaw`。最终 iteration 2998 指标：Mean teacher reward `23.86`，Mean teacher episode length `1201.58`，Mean student reward `24.48`，Mean student episode length `1236.57`，`Episode_Termination/time_out=0.9512`，`Episode_Termination/illegal_contact=0.0488`，`Curriculum/terrain_levels=5.8810`。相比第一轮 1000 iteration，student reward 从 `5.32` 提升到 `24.48`，illegal contact 从 `0.4512` 降到 `0.0488`，说明继续训练后策略明显稳定；下一步应加载 `model_2998.pt` 做 GUI 回放，确认实际步态和地形通过形态。

- [x] 2026-06-15: 完成 `--num_envs=512` 续训测试。命令：`conda run -n env_isaaclab python scripts/rsl_rl/train.py --task=RobotLab-DMBot-v0 --headless --num_envs=512 --max_iterations=1000 --resume --load_run 2026-06-15_13-07-09_dmbot_resume_to_3000_256envs --checkpoint model_2998.pt --logger wandb --log_project_name Robot_house --run_name dmbot_resume_512envs_1000`。训练正常退出，日志目录 `logs/rsl_rl/dmbot_moe_cts/2026-06-15_13-57-19_dmbot_resume_512envs_1000`，checkpoint 已生成 `model_3000.pt`、`model_3500.pt`、`model_3997.pt`；wandb run 地址 `https://wandb.ai/0592bt-carnegie-mellon-university/Robot_house/runs/03itcy1h`。最终 iteration 3997 指标：Mean teacher reward `25.88`，Mean student reward `25.50`，Mean student episode length `1219.05`，`Episode_Termination/time_out=0.9480`，`Episode_Termination/illegal_contact=0.0540`，`Curriculum/terrain_levels=5.2070`，`Steps per second=8087`。分段对比：256 envs 上一轮最后 100 iteration 均值为 `8978 steps/s`、student reward `24.32`、illegal contact `0.069`；512 envs 本轮最后 100 iteration 均值为 `8397 steps/s`、student reward `25.04`、illegal contact `0.058`。结论：512 envs 可稳定训练，显存峰值约 `14.3GB/16.3GB`、GPU 利用率约 `83-85%`，但吞吐没有高于 256 envs；512 更像提高 batch/稳定性，而不是加速训练。训练启动日志还提示 CPU performance profile 为 `powersave`、PCIe link current generation 与 maximum 不一致，后续若追求训练速度，应优先检查系统性能模式/PCIe 链路，而不是继续盲目增大 env 数。

- [x] 2026-06-15: 按 Stage 1 打开 DMBot parallel multi-terrain 配置。已将 `source/robot_lab/robot_lab/tasks/dmbot/mdp/terrains.py` 从 flat-only 改为 `flat=0.50`、`slope_up=0.20`、`slope_down=0.20`、`rough_slope=0.10`，slope/rough_slope 难度先限制为 `slope_range=(0.05, 0.25)`，rough noise 降为 `(-0.02, 0.02)`，暂不启用 stairs/obstacles/wave/gap。同步更新 `tests/test_dmbot_task_config.py`，验证 Stage 1 地形组合并禁止更高难度地形提前进入。验证：`python -m py_compile source/robot_lab/robot_lab/tasks/dmbot/mdp/terrains.py tests/test_dmbot_task_config.py` 通过；`conda run -n env_isaaclab pytest tests/test_dmbot_task_config.py -q` 结果 `4 passed`；AppLauncher 导入检查输出 `TERRAIN_ITEMS [('flat', 0.5), ('slope_up', 0.2), ('slope_down', 0.2), ('rough_slope', 0.1)]`，比例总和约 `1.0`。下一步可从 `2026-06-15_14-45-59_dmbot_flat_extra_5000_from3997_512envs/model_6000.pt` 继续训练 3000 iteration，run name 建议 `dmbot_stage1_flat_slope_rough_from6000_512envs`。

- [x] 2026-06-15: 完成 Stage 1 parallel multi-terrain 训练。命令为从 `2026-06-15_14-45-59_dmbot_flat_extra_5000_from3997_512envs/model_6000.pt` 继续，`--num_envs=512 --max_iterations=3000 --run_name dmbot_stage1_flat_slope_rough_from6000_512envs`。训练正常跑到 `Learning iteration 8999/9000`，日志目录 `logs/rsl_rl/dmbot_moe_cts/2026-06-15_16-06-24_dmbot_stage1_flat_slope_rough_from6000_512envs`，checkpoint 已生成 `model_6000.pt`、`model_6500.pt`、`model_7000.pt`、`model_7500.pt`、`model_8000.pt`、`model_8500.pt`、`model_8999.pt`；wandb run 地址 `https://wandb.ai/0592bt-carnegie-mellon-university/Robot_house/runs/uacadr2s`。最终 iteration 8999 指标：Mean teacher reward `28.01`，Mean student reward `27.69`，Mean student episode length `1213.17`，`Episode_Termination/time_out=0.9733`，`Episode_Termination/illegal_contact=0.0267`，`Curriculum/terrain_levels=5.4259`，`Steps per second=7833`。最后 100 iteration 均值：student reward `27.14`，illegal contact `0.036`，time_out `0.964`，terrain level `5.59`。结论：加入 `flat+slope_up+slope_down+rough_slope` 后没有出现崩溃，非法接触继续下降，策略在 Stage 1 多地形上基本稳定；下一步应先 GUI 回放 `model_8999.pt`，确认斜坡和粗糙斜坡上的实际步态，再决定是否加小台阶 stairs。

- [x] 2026-06-15: 按用户要求将 DMBot 默认姿态的 pitch/knee 两类关节改为 Go2 对应幅值，但保持 DMBot 当前 `hipr=0.0` 和左右镜像符号规则。`source/robot_lab/robot_lab/assets/dmbot.py` 已改为：前腿 `hipp` 幅值 `0.8`，后腿 `hipp` 幅值 `1.0`，四个 `knee` 幅值 `1.5`；即左侧 `hipp/knee` 为负，右侧为正。同步更新 `tests/test_dmbot_asset_files.py` 和 `scripts/tools/write_dmbot_usd_stance.py`，并重新执行 `write_dmbot_usd_stance.py`、`bake_dmbot_usd_stance.py`、`flatten_dmbot_usd_stance.py`，让训练用 stance USD 与 flattened 预览文件都反映新姿态。TDD 验证：默认姿态单测和 USD stance 脚本单测先在旧值上失败，修改后通过；最终 `conda run -n env_isaaclab pytest tests/test_dmbot_asset_files.py tests/test_dmbot_task_config.py tests/test_dmbot_usd_stance_script.py -q` 结果 `14 passed`；`write_dmbot_usd_stance.py --verify-only` 和 `bake_dmbot_usd_stance.py --verify-only` 退出码均为 0；`visual_check_dmbot.py --headless --duration 1 --no-real-time` 成功加载，`t=0.0s` base 高度约 `0.345m`，root quaternion 接近正向。

- [x] 2026-06-15: 按用户要求验证“Go2 角度幅值”是否真的让 DMBot 几何姿态接近 Go2。新增 `scripts/tools/compare_dmbot_go2_reset_geometry.py` 和 `tests/test_compare_reset_geometry_script.py`，脚本通过两个独立 IsaacLab headless 子进程分别采样 `RobotLab-Go2-v0` 与 `RobotLab-DMBot-v0` 的 reset 状态，关闭 reset/startup 随机化后比较四个 foot body 在各自 base frame 下的 `(x, y, z)`。验证：测试先因脚本缺失失败；实现后 `conda run -n env_isaaclab pytest tests/test_compare_reset_geometry_script.py -q` 结果 `1 passed`，`python -m py_compile scripts/tools/compare_dmbot_go2_reset_geometry.py tests/test_compare_reset_geometry_script.py` 通过；真实诊断命令 `conda run -n env_isaaclab python scripts/tools/compare_dmbot_go2_reset_geometry.py --headless` 成功输出：DMBot 相对 Go2 的 `mean_abs_delta xyz=(0.3738, 0.0155, 0.2184)`，stance length 比 Go2 长 `0.0370m`、width 宽 `0.0311m`，但四个脚整体在 x 方向向后约 `0.36-0.39m`，z 方向离 base 更近约 `0.218m`。结论：当前只按 Go2 角度幅值改 DMBot，不能保证实际站姿几何接近 Go2；若目标是“像 Go2 那样站”，下一步应按 foot position/base-frame 几何反向调 DMBot 的 `hipp/knee` 符号和幅值，而不是继续只复制 Go2 的角度数值。

- [x] 2026-06-15: 按用户要求将从头 5w 训练切到更难地形和更快速度课程。`source/robot_lab/robot_lab/tasks/dmbot/mdp/terrains.py` 的 active `TERRAIN_CFG` 已改为 Stage 2-Hard：`flat=0.20`，`slope_up=0.18`、`slope_down=0.18` 且 `slope_range=(0.10, 0.50)`，`rough_slope=0.14` 且 `slope_range=(0.10, 0.45)`、`noise_range=(-0.05, 0.05)`，`random_rough=0.10`，`stairs_up=0.12`，`stairs_down=0.05`，`obstacles=0.03`；暂不启用 `wave/stepping_stones/gap`。`source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py` 已将初始命令范围提高到 `lin_vel_x=[-0.8,0.8]`、`lin_vel_y=[-0.6,0.6]`、`ang_vel_yaw=[-1.2,1.2]`，课程提前为 `5000 -> [-1.0,1.0]`，`20000 -> [-1.5,1.5]`，`35000 -> [-2.0,2.0]`；斜坡地形速度上限放到 `1.8m/s`，粗糙地形 `1.5m/s`，楼梯 `1.2m/s`。验证：更新测试先在旧配置上失败；修改后 `conda run -n env_isaaclab pytest tests/test_dmbot_task_config.py -q` 结果 `5 passed`，`python -m py_compile source/robot_lab/robot_lab/tasks/dmbot/mdp/terrains.py source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py tests/test_dmbot_task_config.py` 通过；`AppLauncher(headless=True)` 运行时导入检查打印 `CHECK_TERRAINS [('flat', 0.2), ('slope_up', 0.18), ('slope_down', 0.18), ('rough_slope', 0.14), ('random_rough', 0.1), ('stairs_up', 0.12), ('stairs_down', 0.05), ('obstacles', 0.03)]` 和初始速度 `[-0.8, 0.8] [-0.6, 0.6] [-1.2, 1.2]`。风险：这是从 Stage 1 直接加难，若前 2k-5k iteration `illegal_contact` 很高或 episode length 上不来，应回退到 Stage 1.5 或先降低楼梯/障碍比例。

- [x] 2026-06-16: 按用户要求切换到 Go2 原始策略对比版，只改 3 个变量：地形配置、初始地形难度等级、速度课程。`source/robot_lab/robot_lab/tasks/dmbot/mdp/terrains.py` 的 active `TERRAIN_CFG` 已对齐 Go2 原始地形分布：`wave=0.05`、`slope_up=0.10`、`slope_down=0.10`、`rough_slope=0.05`、`stairs_up=0.25`、`stairs_down=0.10`、`obstacles=0.20`、`stepping_stones=0.0`、`gap=0.0`、`flat=0.15`，并恢复 Go2 的斜坡、台阶、障碍高度范围。`source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py` 已将 `max_init_terrain_level` 从 `0` 改为 Go2 的 `5`。`source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py` 已恢复 Go2 原速度课程：初始 `lin_vel_x/y=[-0.5,0.5]`、`ang_vel_yaw=[-1.0,1.0]`，`20000 -> x/y=[-1,1], yaw=[-1.5,1.5]`，`50000 -> x=[-2,2], y=[-1,1], yaw=[-2,2]`；斜坡/粗糙/波浪速度上限恢复 `x=[-1.5,1.5]`，楼梯/障碍恢复 `x=[-1.0,1.0]`，平地保持 `x=[-2.0,2.0]`。算法、奖励、DMBot 资产和默认站姿未改。验证：测试先在旧 Stage 2-Hard/Fast 配置上失败；修改后 `python -m py_compile source/robot_lab/robot_lab/tasks/dmbot/mdp/terrains.py source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py tests/test_dmbot_task_config.py` 通过，`conda run -n env_isaaclab pytest tests/test_dmbot_task_config.py -q` 结果 `5 passed`。风险：这次对比版地形一开始就包含较多 Go2 原始 stairs/obstacles，且初始 terrain level 为 `5`，比之前 DMBot 平滑递进更硬；若从头训练前几千轮 `illegal_contact` 偏高，需要和 Stage2-Hard/Fast 指标分开比较，不应混在同一个结论里。

- [x] 2026-06-16: 针对 `2026-06-16_11-12-36/model_4000.pt` 低速阶段平台期，切换为二阶段续训配置。原因：用户观察到 adaptive learning rate 已逼近低值、teacher/student reward 在 `max_command_x=0.5` 低速范围内增长放缓；由于 resume 后环境 `common_step_counter` 会从 0 重新计算，单纯等待原 `20000` 速度课程会重放低速阶段。已将 `source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py` 的初始命令范围改为二阶段：`lin_vel_x/y=[-1.0,1.0]`、`ang_vel_yaw=[-1.5,1.5]`，并将最终高速节点设为新 run 的 `20000 -> x=[-2.0,2.0], y=[-1.0,1.0], yaw=[-2.0,2.0]`；同时将 `zero_command_curriculum` 固定为 `0.1 -> 0.1`，等价于 Go2 连续训练超过 1500 iteration 后的零命令概率，避免从 0 到 0.1 的零命令概率课程在续训中重放。`source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py` 已将 `lin_vel_z_l2` 固定为 `-0.0`、`base_height_l2` 固定为 `-8.2`，其中 `-8.2` 近似对应原 base height 课程在 4000 iteration 的自然权重；对应 reward weight curriculum 设为 `None`，避免辅助 reward 课程在 resume 后重新从早期权重开始；`terrain_levels` 继续保留。验证：测试先在旧低速配置和重放 reward curriculum 上失败；修改后 `python -m py_compile source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py tests/test_dmbot_task_config.py` 通过，`conda run -n env_isaaclab pytest tests/test_dmbot_task_config.py -q` 结果 `6 passed`。该配置不再是严格 Go2 原始课程，而是 DMBot 从 4000 iteration checkpoint 进入二阶段速度的观察性续训配置。

- [x] 2026-06-16: 回退二阶段续训配置，恢复到 `2026-06-16_11-12-36` 这次 4000 iteration 训练启动时的 Go2 原始课程状态。失败分支为 `logs/rsl_rl/dmbot_moe_cts/2026-06-16_17-02-36_dmbot_resume_stage2_speed_from4000_5000_6144envs`，其末尾 `max_command_x=1.0` 时指标明显退化：teacher reward 约 `10`、student reward 约 `8`、action noise std 约 `0.46`、illegal_contact 约 `0.28`，相比原 `model_4000` 附近的 teacher reward 约 `22.6`、student reward 约 `19.1`、noise std 约 `0.33`、illegal_contact 约 `0.109` 明显变差，因此不建议继续使用该分支的 `model_4500.pt/model_5000.pt`。已回退 `source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py`：初始速度恢复为 `lin_vel_x/y=[-0.5,0.5]`、`ang_vel_yaw=[-1.0,1.0]`，速度课程恢复为 `20000 -> [-1,1]/yaw[-1.5,1.5]` 和 `50000 -> x[-2,2], y[-1,1], yaw[-2,2]`，`zero_command_curriculum` 恢复为 `0.0 -> 0.1 / 1500`。已回退 `source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py`：`lin_vel_z_l2` 恢复为 `-2.0`，`base_height_l2` 恢复为 `-1.0`，对应 `gradual_reward_weight_modification` 课程恢复，`terrain_levels` 保留。验证：配置测试先在二阶段配置下失败；回退后 `python -m py_compile source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py tests/test_dmbot_task_config.py` 通过，`conda run -n env_isaaclab pytest tests/test_dmbot_task_config.py -q` 结果 `6 passed`。

- [x] 2026-06-16: 按用户要求导出并打包二阶段续训分支的 `model_5000.pt`，用于发给同事临时回放/对比。确认 `logs/rsl_rl/dmbot_moe_cts/2026-06-16_17-02-36_dmbot_resume_stage2_speed_from4000_5000_6144envs/exported/` 下已有 `policy.pt` 和 `policy.onnx`；已生成压缩包 `/tmp/dmbot_model_5000_export.tar.gz`，大小约 `35M`，内容包含 `model_5000.pt`、`params/agent.yaml`、`params/env.yaml`、`exported/policy.pt`、`exported/policy.onnx`。注意：该模型视觉效果可用于临时检查，但训练指标相比 `model_4000.pt` 明显退化，不建议作为后续主线续训起点。

- [x] 2026-06-16: 按用户要求重新切回 `model_5000.pt` 所属二阶段续训分支的课程配置，以便从 `logs/rsl_rl/dmbot_moe_cts/2026-06-16_17-02-36_dmbot_resume_stage2_speed_from4000_5000_6144envs/model_5000.pt` 继续训练时环境设置与该分支保持一致。`commands.py` 已恢复为初始 `lin_vel_x/y=[-1.0,1.0]`、`ang_vel_yaw=[-1.5,1.5]`，速度课程为新 run 的 `20000 -> x=[-2.0,2.0], y=[-1.0,1.0], yaw=[-2.0,2.0]`，`zero_command_curriculum` 固定为 `0.1 -> 0.1`。`env_cfg.py` 已恢复为 `lin_vel_z_l2=-0.0`、`base_height_l2=-8.2`，并关闭 `base_linear_velocity` 与 `base_height_l2` 的辅助 reward weight curriculum，保留 `terrain_levels`。验证：先更新测试并确认在回退配置上失败；修改后 `conda run -n env_isaaclab pytest tests/test_dmbot_task_config.py -q` 结果 `6 passed`，`python -m py_compile source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py tests/test_dmbot_task_config.py` 通过，`git diff --check` 无输出。

- [x] 2026-06-16: 按用户要求将 `model_5000.pt` 续训配置中的 `base_height_l2` 调整得更贴近原始课程自然状态。原始 Go2 辅助课程会在 `0 -> 5000` iteration 将 `base_height_l2` 从 `-1.0` 增加到 `-10.0`，因此从 `model_5000.pt` 继续时更合理的固定值是 `-10.0`，而不是此前近似 `model_4000.pt` 状态的 `-8.2`。已将 `source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py` 中 `base_height_l2` weight 从 `-8.2` 改为 `-10.0`，仍保持 `base_height_l2 = None`，避免 resume 后从 `-1.0` 重放课程。验证：测试先在 `-8.2` 上失败；修改后 `conda run -n env_isaaclab pytest tests/test_dmbot_task_config.py -q` 结果 `6 passed`，`python -m py_compile source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py tests/test_dmbot_task_config.py` 通过，`git diff --check` 无输出。

- [x] 2026-06-17: 修复多 env GUI 回放时 `--no-camera-follow` 仍像贴着单只机器狗的问题。原因是该参数此前只关闭每帧跟随，但没有主动把 Isaac Sim 初始视角从 `env_0` 附近移到全局视角。已在 `scripts/rsl_rl/play.py` 新增 `camera_overview(env)`：当启用 `--no-camera-follow` 时，根据 `scene.env_origins` 计算所有 env 的中心和跨度，启动后只设置一次高空 overview 相机，然后不再碰相机，便于手动自由移动摄像机观察多只机器狗和速度箭头。验证：新增测试先因 `camera_overview` 缺失失败；实现后 `conda run -n env_isaaclab pytest tests/test_play_camera_follow.py -q` 结果 `2 passed`，`python -m py_compile scripts/rsl_rl/play.py tests/test_play_camera_follow.py` 通过，`git diff --check` 无输出。

- [x] 2026-06-17: 新增回放专用高难度地形参数 `--terrain-level`。在 `scripts/rsl_rl/play.py` 中新增 `force_terrain_level(env, terrain_level)`，创建环境后将 `terrain.terrain_levels` 全部设为指定行，并用 `terrain.terrain_origins[terrain_levels, terrain_types]` 重写 `terrain.env_origins`，随后调用 `env.reset()` 让机器人实际生成到对应难度地形上。指定该参数时会关闭 `env_cfg.curriculum.terrain_levels`，避免回放过程中又被课程移动到其他难度。典型用法：`--terrain-level 9` 强制最高难度行。验证：新增测试先因参数缺失失败；实现后 `conda run -n env_isaaclab pytest tests/test_play_camera_follow.py -q` 结果 `3 passed`，`python -m py_compile scripts/rsl_rl/play.py tests/test_play_camera_follow.py` 通过，`git diff --check` 无输出。

- [x] 2026-06-17: 按用户纠正，将回放地形覆盖从“全部最高难度”扩展为“所有课程等级都有”。`--terrain-level` 现在支持两种形式：`--terrain-level 9` 仍表示全部 env 放到指定 level；`--terrain-level all` 表示按 env id 在 `0..max_level` 之间循环分配，确保回放时同时看到低、中、高所有 terrain curriculum level。terrain type 列分布仍沿用 terrain generator 原逻辑，因此可以同时覆盖不同地形类型和不同难度行。验证：测试先在旧实现上失败；修改后 `conda run -n env_isaaclab pytest tests/test_play_camera_follow.py -q` 结果 `4 passed`，`python -m py_compile scripts/rsl_rl/play.py tests/test_play_camera_follow.py` 通过，`git diff --check` 无输出。

- [x] 2026-06-17: 按用户确认，将从 `2026-06-16_19-00-57/model_22000.pt` 继续训练的速度课程改为中间过渡版。`source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py` 仍以 `lin_vel_x/y=[-1.0,1.0]`、`ang_vel_yaw=[-1.5,1.5]` 起步；新 run 第 `2000` iteration 扩到 `lin_vel_x=[-1.5,1.5]`、`lin_vel_y=[-1.0,1.0]`、`ang_vel_yaw=[-1.75,1.75]`；第 `8000` iteration 扩到 `lin_vel_x=[-2.0,2.0]`、`lin_vel_y=[-1.0,1.0]`、`ang_vel_yaw=[-2.0,2.0]`。这样比直接从 `1.0 -> 2.0` 更平滑，同时避免继续在 `level≈5`、`max_command_x=1.0` 上长时间平台。验证：测试先在旧单段课程上失败；修改后 `conda run -n env_isaaclab pytest tests/test_dmbot_task_config.py -q` 结果 `6 passed`，`python -m py_compile source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py tests/test_dmbot_task_config.py` 通过；本次触及文件的 `git diff --check` 通过。全仓 `git diff --check` 仍报告既有 `deploy/deploy_mujoco/configs/go2.yaml` trailing whitespace，未在本步骤修改。

- [x] 2026-06-17: 补充记录 `model_22000.pt` 续训速度课程的 checkpoint 换算和地形限速解释。由于课程按新 run 内部 iteration 计算，从 `model_22000.pt` 继续时，第 `2000` 个新 iteration 大约对应 `model_24000.pt`，速度扩到 `lin_vel_x=[-1.5,1.5]`；第 `8000` 个新 iteration 大约对应 `model_30000.pt`，速度扩到 `lin_vel_x=[-2.0,2.0]`。但 `terrain_max_command_ranges` 仍会按地形类型裁剪最终命令范围：`flat` 允许 `x=[-2.0,2.0]`，`wave/slope_up/slope_down/rough_slope` 上限为 `x=[-1.5,1.5]`，`stairs_up/stairs_down/obstacles/stepping_stones/gap` 上限为 `x=[-1.0,1.0]`。因此即使全局课程到 `2.0`，复杂地形上的命令仍不会全部达到 `2.0m/s`。

- [x] 2026-06-17: 按用户要求将 DMBot 训练逻辑全面退回到最初 Go2 原版策略。保留 DMBot 资产、关节名、接触 body 正则、USD 和回放工具不变，只恢复训练课程/奖励调度：`commands.py` 初始速度回到 `lin_vel_x/y=[-0.5,0.5]`、`ang_vel_yaw=[-1.0,1.0]`，速度课程回到第 `20000` iteration 扩到 `x/y=[-1.0,1.0]`、`yaw=[-1.5,1.5]`，第 `50000` iteration 扩到 `x=[-2.0,2.0]`、`y=[-1.0,1.0]`、`yaw=[-2.0,2.0]`；`zero_command_curriculum` 回到 `0.0 -> 0.1`。`env_cfg.py` 中 `lin_vel_z_l2` 初始权重回到 `-2.0`，`base_height_l2` 初始权重回到 `-1.0`，并恢复 Go2 原版辅助课程：`lin_vel_z_l2` 在 `0..1500` iteration 从 `-2.0` 过渡到 `-0.0`，`base_height_l2` 在 `0..5000` iteration 从 `-1.0` 过渡到 `-10.0`。地形配置此前已经与 Go2 原版一致：`num_rows=10`、`num_cols=20`、`max_init_terrain_level=5`，地形比例为 `wave=0.05`、`slope_up=0.10`、`slope_down=0.10`、`rough_slope=0.05`、`stairs_up=0.25`、`stairs_down=0.10`、`obstacles=0.20`、`flat=0.15`，`stepping_stones/gap=0.0`。注意：已生成的 `model_26000.pt` 仍是旧加速课程 run 的产物；本次修改只影响之后新启动的训练/续训。验证：先更新测试让其要求 Go2 原版课程并确认旧代码失败；修改后 `conda run -n env_isaaclab pytest tests/test_dmbot_task_config.py -q` 结果 `6 passed`，`python -m py_compile source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py source/robot_lab/robot_lab/tasks/dmbot/env_cfg.py tests/test_dmbot_task_config.py` 通过。

- [x] 2026-06-17: 记录新的训练部署状态。用户当前已有公司租用的 A100 服务器可用于训练，下一步目标是把当前 DMBot 迁移版仓库、DMBot USD 资产、当前 Go2 原版训练逻辑配置和 W&B 记录方式迁移到公司 GPU 上运行 headless 训练。当前仓库没有现成 Dockerfile/docker-compose；README 提供的是 conda + pip 安装 IsaacLab `2.3.2.post1`、PyTorch `2.7.0/cu128`，再 editable 安装 `source/robot_lab` 和 `source/rsl_rl` 的路线。由于 DMBot 任务和资产在本地工作区中仍包含未提交/新增文件，迁移服务器时不能只 clone 原始 GitHub 仓库，必须同步当前完整工作区或先整理成 commit/压缩包再传到服务器。

- [x] 2026-06-17: 用户决定本地从头训练一条完整 Go2 原版逻辑 DMBot run，不再从 `2026-06-16_11-12-36/model_4000.pt` 继续，以避免 resume 后 `common_step_counter` 重新从 0 导致速度/奖励课程重放与 checkpoint iteration 不完全对齐。计划使用 `RobotLab-DMBot-v0`、`--num_envs=6144`、`--max_iterations=100000`、W&B project `isaaclab`，run name 建议 `dmbot_go2_original_from_scratch_100000_6144envs`。训练课程为完整从头：`0..20000` 低速，`20000..50000` 中速，`50000..100000` 高速；保存间隔仍为 500 iteration。

- [x] 2026-06-18: 评估本地从头训练 run `2026-06-17_19-15-47_dmbot_go2_original_from_scratch_100000_6144envs` 的 20000 iteration 速度课程切换。切换前 `19000..19900` 时 `max_command_x=0.5`，teacher reward 约 `22.9..23.8`、student reward 约 `19.9..22.6`、`illegal_contact≈0.076..0.079`、`time_out≈0.92`、terrain level 约 `4.9..5.0`。切换到 `max_command_x=1.0` 后，`20100` 附近 teacher/student reward 降到约 `11.9/9.4`，`illegal_contact` 升到约 `0.224`，`time_out` 降到约 `0.776`；到 `23000..23123` 仍未恢复到切换前水平，teacher reward 约 `11..12`，student reward 约 `8.5..10.9`，`illegal_contact≈0.21..0.22`，terrain level 仍约 `5.14`。判断：Go2 原版的 `0.5 -> 1.0` 速度跳变对 DMBot 是明显冲击，但尚未完全崩溃；若继续使用原版课程，50000 的 `1.0 -> 2.0` 很可能更激进。后续可考虑延后速度切换或插入 `0.75/1.0/1.5/2.0` 的中间阶段。

- [x] 2026-06-18: 按用户要求将 DMBot 速度课程改为“保持 Go2 原始最终目标不变，但更平缓”的版本。保留初始 `lin_vel_x/y=[-0.5,0.5]`、`ang_vel_yaw=[-1.0,1.0]`，保留最终目标 `lin_vel_x=[-2.0,2.0]`、`lin_vel_y=[-1.0,1.0]`、`ang_vel_yaw=[-2.0,2.0]`，但将原来的 `20000 -> 1.0`、`50000 -> 2.0` 两段大跳变拆成：`20000 -> x/y=[-0.75,0.75], yaw=[-1.25,1.25]`；`35000 -> x/y=[-1.0,1.0], yaw=[-1.5,1.5]`；`60000 -> x=[-1.5,1.5], y=[-1.0,1.0], yaw=[-1.75,1.75]`；`100000 -> x=[-2.0,2.0], y=[-1.0,1.0], yaw=[-2.0,2.0]`。地形配置、地形限速、zero command 课程和辅助 reward 课程不变。注意：该修改只影响之后新启动的训练 run；已有 `2026-06-17_19-15-47_dmbot_go2_original_from_scratch_100000_6144envs` checkpoint 仍来自旧 Go2 原版速度课程。验证：先更新 `tests/test_dmbot_task_config.py` 期望并确认旧源码失败；修改 `source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py` 后，`conda run -n env_isaaclab pytest tests/test_dmbot_task_config.py -q` 结果 `6 passed`，`python -m py_compile source/robot_lab/robot_lab/tasks/dmbot/mdp/commands.py tests/test_dmbot_task_config.py` 通过。

- [x] 2026-06-22: 检查平滑速度课程长训 run `2026-06-18_18-53-05_dmbot_smooth_speed_from_scratch_200000_6144envs`。`train.log` 最新写到 `Learning iteration 104987/200000`，当前已经进入最终速度课程，`Metrics/base_velocity/max_command_x=2.0`；最新已保存 checkpoint 为 `model_104500.pt`。该 run 的本地 W&B 目录为 `wandb/run-20260618_185319-9m4mn7nc`，run id 为 `9m4mn7nc`，项目为 `isaaclab`。W&B 断连原因不是训练未记录，而是代理 `127.0.0.1:7890` 在 `2026-06-22 12:01..12:35` 期间连接失败，导致数据缓存在本地；`12:36` 之后日志出现多次 `filestream: request sent status "200 OK"`，说明网络恢复后已经开始补传。若网页端仍缺数据，停止训练后执行 `wandb sync wandb/run-20260618_185319-9m4mn7nc`；若仍报代理错误，则先启动本地代理或临时 `unset HTTP_PROXY HTTPS_PROXY ALL_PROXY http_proxy https_proxy all_proxy` 后再同步。

- [x] 2026-06-22: 对当前最新回放 checkpoint 做 GUI 闪退复现测试。使用 `model_106000.pt`、`RobotLab-DMBot-v0`、`--num_envs=16`、`--fix_commands`、`--no-camera-follow`、`--terrain-level all` 启动 Isaac Sim GUI，并用 `timeout 120s` 限时运行。进程没有自行崩溃退出，最终退出码为 `124`，表示被 `timeout` 正常终止；测试期间生成了新的 `exported/policy.pt` 和 `exported/policy.onnx`，说明 checkpoint 已加载并完成 policy 导出。最新 IsaacLab 日志 `/tmp/isaaclab/logs/isaaclab_2026-06-22_13-51-07.log` 未发现 `Traceback`、`Fatal`、`Segmentation` 等崩溃关键词。结论：该配置至少在 120 秒启动/回放测试内未复现闪退；若用户本机仍闪退，优先检查是否使用完整 checkpoint 路径、GUI 渲染负载、窗口/驱动状态和是否同时还有训练进程占用 GPU。

- [x] 2026-06-22: 用户反馈“刚打开就闪退”后，使用更接近用户终端的方式复测：`source /home/sbt/miniforge3/etc/profile.d/conda.sh; conda activate env_isaaclab; python scripts/rsl_rl/play.py --task=RobotLab-DMBot-v0 --num_envs=16 --checkpoint .../model_106000.pt --fix_commands --no-camera-follow --terrain-level all`。本次完整输出显示 Isaac Sim `Simulation App Startup Complete`，环境创建完成，`[PLAY] Spread playback across terrain levels 0-9`，模型 checkpoint 成功加载，`[PLAY] Camera follow disabled; overview camera set once.`，随后持续运行；最终由测试超时/中断退出，并非程序自行闪退。`nvidia-smi` 复查显示测试结束后无残留 Isaac 进程，GPU 显存约 `953MiB/16303MiB`，训练进程也不在运行。当前本机无法复现启动即闪退；若用户仍复现，需要保留终端最后报错或新的 Kit 日志路径来判断差异。

- [x] 2026-06-22: 新增 DMBot 的 MuJoCo Sim2Sim 初版。新增 `deploy/deploy_mujoco/configs/dmbot.yaml`，指向当前长训 run 的 `exported/policy.pt` 和 RoboGauge DMBot MJCF；新增 `deploy/deploy_mujoco/dmbot_scene.py`，运行时从固定基座的 `external/RoboGauge/resources/robots/dmbot/dmbot.xml` 生成带 `freejoint` 和地面的临时 scene，避免直接修改原始资产；新增 `deploy/deploy_mujoco/deploy_dmbot.py`，复用 Go2 deploy 的 CTS policy、PD 控制、手柄命令和速度箭头逻辑，但使用 DMBot 关节名、默认角度、`base_init_pos=[0,0,0.35]`、`simulation_dt=0.005`、`control_decimation=4`、`kp=40`、`kd=2`。验证：先新增 DMBot 配置/scene 测试并确认失败；实现后 `pytest tests/test_deploy_policy_config.py -q` 结果 `4 passed`，无 GUI smoke test 显示生成后的模型 `nq=19/nv=18/nu=12`、single obs 为 45 维、policy 输出 12 维 action；`python -m py_compile deploy/deploy_mujoco/dmbot_scene.py deploy/deploy_mujoco/deploy_dmbot.py tests/test_deploy_policy_config.py` 通过；相关文件 `git diff --check` 通过。注意：目前只验证了模型/策略可加载和单步推理，MuJoCo viewer 中的真实步态质量还需要实际运行 `deploy_dmbot.py` 观察。

- [x] 2026-06-22: 按用户要求将 DMBot MuJoCo 场景从简单平地扩展为训练地形代表场景。`deploy/deploy_mujoco/configs/dmbot.yaml` 新增 `terrain_profile: "training_mixed"`；`dmbot_scene.py` 会根据训练配置中实际启用的 terrain 类型生成代表性 MuJoCo geoms：`wave`、`slope_up`、`slope_down`、`rough_slope`、`stairs_up`、`stairs_down`、`obstacles` 和 `flat`，其中 `stepping_stones/gap` 由于训练比例为 `0.0` 暂不加入。该实现使用可解析的 MuJoCo box/plane 几何近似 IsaacLab heightfield，台阶/障碍/斜坡尺度参考训练配置范围，但不是逐格复刻 IsaacLab terrain generator。验证：先新增 `training_mixed` 测试并确认失败；实现后 `pytest tests/test_deploy_policy_config.py -q` 结果 `6 passed`；无 GUI smoke test 显示生成场景 `terrain_profile=training_mixed`、`nq=19/nv=18/nu=12/ngeom=50`，并确认 `training_slope_up`、`training_stairs_up_0`、`training_obstacle_0`、`training_wave_0` 等地形存在，policy 仍可用 45 维 single obs 输出 12 维 action；`python -m py_compile deploy/deploy_mujoco/dmbot_scene.py deploy/deploy_mujoco/deploy_dmbot.py deploy/deploy_mujoco/utils.py tests/test_deploy_policy_config.py` 通过；相关文件 `git diff --check` 通过。

- [x] 2026-06-22: 修复 DMBot MuJoCo 训练代表地形中上下坡和台阶主路线存在断缝的问题。根因是生成参数让 `training_slope_up` 的 x 范围结束于 `3.5`，而 `training_slope_down` 从 `3.7` 才开始，中间有 `0.2m` 空档；同时下坡到上台阶、上台阶到下台阶也存在不必要间隔。已将下坡块左移并让台阶起点前移，移除上下台阶之间的额外 `0.45m` 间隔，使主路线相邻地形段连续或轻微重叠。新增 `test_dmbot_training_mixed_main_path_has_no_terrain_gaps` 防止后续回归；验证：该测试先在旧参数上失败，修复后通过；完整 `pytest tests/test_deploy_policy_config.py -q` 结果 `7 passed, 1 warning`；`python -m py_compile deploy/deploy_mujoco/dmbot_scene.py deploy/deploy_mujoco/deploy_dmbot.py deploy/deploy_mujoco/utils.py tests/test_deploy_policy_config.py` 通过；生成场景 smoke check 显示 `training_slope_up=(1.1,3.5)`、`training_slope_down=(3.4,5.8)`、`training_stairs_up_0=(5.775,6.085)`、`training_stairs_up_4=(7.015,7.325)`、`training_stairs_down_0=(7.325,7.635)`，模型维度仍为 `nq=19/nv=18/nu=12/ngeom=50`。

- [x] 2026-06-22: 按用户要求调整 DMBot MuJoCo deploy 的默认命令，使进入 MuJoCo 后机器狗不再自动前进，而是等待手柄输入。已将 `deploy/deploy_mujoco/configs/dmbot.yaml` 中 `cmd_init` 从 `[0.4, 0.0, 0.0]` 改为 `[0.0, 0.0, 0.0]`，手柄映射和 `max_cmd=[1.0, 0.8, 1.0]` 保持不变。新增 `test_dmbot_mujoco_starts_with_zero_command_for_manual_control` 防止默认命令被误改回自动前进。验证：`pytest tests/test_deploy_policy_config.py -q` 结果 `8 passed, 1 warning`；`python -m py_compile deploy/deploy_mujoco/deploy_dmbot.py deploy/deploy_mujoco/utils.py tests/test_deploy_policy_config.py` 通过；相关文件 `git diff --check` 通过。

- [x] 2026-06-22: 按用户进一步纠正，将 DMBot MuJoCo `training_mixed` 场景后段改为两段 10 级台阶并带顶部平台。`training_stairs_15cm` 现在每一级高度差为 `0.15m`，共 10 级，最终高度 `1.50m`，后接 `1.50m` 高平台；`training_stairs_20cm` 每一级高度差为 `0.20m`，共 10 级，最终高度 `2.00m`，后接 `2.00m` 高平台。更新 `test_dmbot_training_mixed_includes_two_10_step_stair_segments_with_platforms`，要求两段台阶均为 10 级、每级高度正确、相邻台阶无间隙、平台长度至少 `0.8m`。验证：新测试先在旧 3/4 级实现上失败；修改后通过；完整 `pytest tests/test_deploy_policy_config.py -q` 结果 `9 passed, 1 warning`；`python -m py_compile deploy/deploy_mujoco/dmbot_scene.py tests/test_deploy_policy_config.py` 通过；生成场景检查显示 15cm 段第 1 级 `0.15m`、第 10 级和平台 `1.50m`，20cm 段第 1 级 `0.20m`、第 10 级和平台 `2.00m`。

- [x] 2026-06-22: 按用户要求将两段 10 级台阶分开放置，避免机器狗刚下/刚过第一段平台后马上进入第二段导致手柄控制困难。已将 `training_stairs_20cm` 起点从 `x=16.00` 后移到 `x=20.00`，使 `training_stairs_15cm_platform` 结束于 `x=15.55`，`training_stairs_20cm_step_0` 开始于 `x=19.85`，中间约 `4.3m` 空地。同步更新测试，要求两段台阶之间至少 `4.0m` 间隔。验证：新测试先在旧间距 `0.3m` 上失败；修改后 `pytest tests/test_deploy_policy_config.py -q` 结果 `9 passed, 1 warning`；`python -m py_compile deploy/deploy_mujoco/dmbot_scene.py tests/test_deploy_policy_config.py` 通过；生成场景检查确认间隔 `4.3m`。

## 问题记录

- 2026-06-12: 可视化检查中发现两点：一是 `conda run python -c` 的多行 GUI 循环形式会触发 IsaacSim segfault，已改为正式脚本执行；二是 `--flat-plane` 检查卡住，疑似当前 height scanner/terrain 配置与直接切 plane 不兼容，后续需要单独做平地配置而不是临时改 `terrain_type`。

- 2026-06-12: `bake_dmbot_usd_go2_pose.py` 在同一进程里完成姿态写入和验证，但混合 IsaacLab 环境 reset 与 USD flatten 时没有稳定产出 flattened 文件；单独运行一行式 `python -c` 加 `AppLauncher` 检查也触发 IsaacSim segfault。当前处理方式是将 flatten 独立为 `scripts/tools/flatten_dmbot_usd_go2_pose.py`，该脚本按 stance flatten 的已验证模式执行，已成功导出 `OpenDog_novlnk_go2_pose_flattened.usd`。

- 2026-06-15: 旧的 `*_flattened.usd` 文件实际为 ASCII USDA，里面包含二进制 mesh 数据，Isaac Sim 打开时会在 `mesh.points` 附近报 `syntax error at '�'`。修复后保留旧文件不再使用，新的可打开预览文件为 `*_flattened.usdc`。

- 2026-06-15: 用户反馈 `.usdc` 后缀不方便查看。结论：后缀可以统一为 `.usd`，只要实际文件格式是 `USD crate`。已重新生成二进制 crate 格式的同名 `*_flattened.usd` 并覆盖旧 ASCII USDA 文件；旧 `.usdc` 文件仍可能留在目录中，但后续不作为主要打开路径。

- 2026-06-15: `calibrate_dmbot_joints.py --headless --duration 0.2 --no-real-time` 曾在当前环境中 60 秒无输出，已定位为脚本直接切换 `plane` 地形导致 terrain generator 缺失，而 DMBot command/height-scanner 相关逻辑需要 generator 元数据。处理方式：校准脚本保持任务自带 flat terrain generator，不再临时改成直接 plane。

- 2026-06-15: DMBot `play.py` 回放不能用 `--num_envs=1`。当前算法配置为 MoE-CTS，内部按 `teacher_env_ratio=0.75` 将环境划分为 teacher/student；单环境时 teacher 索引为空而 teacher 数量被强制至少为 1，导致程序启动后断言退出。处理方式：回放命令使用 `--num_envs=2` 或更多。

- 2026-06-15: DMBot 与 Go2 的关节角度数值不能直接等价。实际 reset 几何检查显示，虽然 DMBot 已使用 Go2 的前/后腿 pitch 和 knee 幅值，但脚在 base frame 下的位置仍与 Go2 差异很大，尤其是 x 方向整体后移、z 方向腿更收。根因是两套资产的 link zero pose、关节轴方向、腿长/髋位置都不同。后续姿态标定应以 foot position/base-frame 几何为目标，而不是以 Go2 joint angle 字面值为目标。

- 2026-06-12: DMBot 最小训练首次失败，报错 `FileNotFoundError: USD file not found at path at: /home/sbt/Desktop/go2_rl_robotlab-main/resources/Robots/dm/OpenDog_novlnk/OpenDog_novlnk.usd`。根因是计划最初把资产复制到 `source/robot_lab/data/Robots/dm`，但当前项目的 `ISAACLAB_ASSETS_DATA_DIR` 实际指向 `resources`。已改为使用 `resources/Robots/dm`。

- 2026-06-12: 初次运行 `conda run -n env_isaaclab python scripts/tools/inspect_dmbot_usd.py` 失败，报错 `ModuleNotFoundError: No module named 'pxr'`。根因是 `pxr` 由 Isaac Sim/Kit 提供，裸 conda Python 不会自动加载；已改为脚本内先启动 `isaaclab.app.AppLauncher(headless=True)`，再导入 `pxr`，并在 `app.close()` 前 flush stdout。

- 2026-06-12: 当前普通沙箱命令会触发 `bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`。执行读取、测试或复制命令时需要使用提升权限绕过这个环境问题；这不是项目代码错误。

- 2026-06-12: Task 5 短训虽然完整跑完，但 `Episode_Termination/illegal_contact` 在后期仍为 `1.0000`，说明当前 DMBot 平地训练仍频繁因非法接触终止。下一步不建议直接扩大到 1000 iteration；应先检查非法接触 body 规则、默认蹲姿过低和 base height target/奖励设置。

- 2026-06-16: 多地形 GUI 回放出现“闪退”时，最近崩溃日志 `kit_20260616_094022.log` 显示崩溃发生在 Isaac Sim/RTX 材质初始化层：`libneuray.so`、`librtx.mdltranslator.plugin.so`、`librtx.materialdb.plugin.so`，不是 Python policy/reward 直接报错。该次命令还把 `--checkpoint` 指到了 run 目录而不是具体 `model_*.pt` 文件。另一份 IsaacLab 日志显示 Stage 2-Hard terrain mesh 规模为 `1324612 vertices`、`7776264 faces`，GUI 下 `--num_envs=8` 需要渲染较重地形和多个 DMBot，因此更容易触发 Kit/RTX 材质或显存/资源问题。处理建议：先用准确 checkpoint 文件和 `--num_envs=2` 回放，稳定后再试 `4/8`；如 headless 能跑而 GUI 闪退，优先按渲染/材质/地形可视化负载处理，而不是先改训练奖励。
